<Window x:Class="UITest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:UITest"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Background="Blue" Padding="5">
                <StackPanel Orientation="Horizontal" Name="Container">
                    
                </StackPanel>
            </Border>
        </StackPanel>
        <Button Margin="0,280,0,0" Height="30" Click="Button_Click" HorizontalAlignment="Left" Width="236">Add</Button>
        <Button Content="Remove" Height="30" Margin="0,281,0,0" Click="Button_Click_1" HorizontalAlignment="Right" Width="236" />
        <TextBox Height="90" HorizontalAlignment="Left" Margin="39,104,0,0" Name="textBox1" VerticalAlignment="Top" Width="136" BorderBrush="Transparent" Background="Transparent" />
        <Button Content="Button" Height="26" HorizontalAlignment="Left" Margin="136,224,0,0" Name="button1" VerticalAlignment="Top" Width="99" Click="button1_Click" />
    </Grid>
</Window>
